define fab(a : Int) : Int =
  if a == 0:
    return 0
  if a == 1:
    return 1
  return fab(a - 1) + fab(a - 2)

! echo "${fab(0)}"
! echo "${fab(1)}"
! echo "${fab(2)}"
! echo "${fab(3)}"
! echo "${fab(4)}"
! echo "${fab(5)}"

